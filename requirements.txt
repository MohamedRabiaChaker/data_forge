# Minimal Python dependencies for Airflow
# Add your custom dependencies here

# Required for PostgreSQL connection (Airflow metadata DB)
psycopg2-binary
asyncpg

# Required Airflow providers for Celery executor and authentication
apache-airflow-providers-celery>=3.3.0
apache-airflow-providers-fab