# Airflow Environment Configuration
# Copy this file to .env and update with your values

# Airflow Web UI Credentials
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# Optional: Set custom UID for file permissions
# AIRFLOW_UID=50000

# ============================================
# ETL Framework Connection Configuration
# ============================================
# Configure these connections in Airflow UI: Admin > Connections
# Or use Airflow CLI or environment variables

# PostgreSQL Connection (for ETL destination)
# Connection ID: postgres_default
# Connection Type: Postgres
# Host: your_postgres_host
# Schema: your_database
# Login: your_username
# Password: your_password
# Port: 5432
# Example CLI command:
# airflow connections add 'postgres_default' \
#   --conn-type 'postgres' \
#   --conn-host 'your_postgres_host' \
#   --conn-schema 'your_database' \
#   --conn-login 'your_username' \
#   --conn-password 'your_password' \
#   --conn-port 5432

# Google Cloud Connection (for Google Sheets source)
# Connection ID: google_cloud_default
# Connection Type: Google Cloud
# Keyfile JSON: {your service account JSON content}
# Scopes: https://www.googleapis.com/auth/spreadsheets.readonly
# Example CLI command:
# airflow connections add 'google_cloud_default' \
#   --conn-type 'google_cloud_platform' \
#   --conn-extra '{"keyfile_dict": {...your service account JSON...}, "scope": "https://www.googleapis.com/auth/spreadsheets.readonly"}'
